# T05 · 고정 검사 10개 (작업 시작 전 확정, 이후 변경 금지)

**대상 기능**: 통화 선택 추가 — USD 대비 KRW/EUR/JPY/GBP 중 선택해서 현재값·출처·날짜별
기록·어제 대비 비교까지 전부 선택한 통화 기준으로 보이게 한다.

**기준 코드**: `ex_5` 저장소, 시작 버전 `777a42d` (baseline: T04 앱 코드 복사)

| ID | 입력 (조작) | 관찰 가능한 기대값 |
|---|---|---|
| T-01 | 페이지를 처음 로드한다 (통화 선택을 건드리지 않음) | `#currency-select`의 선택값이 `"KRW"`이다 |
| T-02 | 통화 선택을 `"EUR"`로 바꾼다 | `#unit`의 텍스트가 `"EUR / 1 USD"`로 바뀐다 |
| T-03 | 통화 선택을 `"EUR"`로 바꾼다 | `#source-link`의 `href`에 `symbols=EUR`이 포함된다 |
| T-04 | 통화 선택을 `"JPY"`로 바꾼다 | 화면에 표시된 값이 `https://api.frankfurter.dev/v1/latest?base=USD&symbols=JPY` 응답의 `rates.JPY` 값(소수 둘째 자리 반올림)과 일치한다 |
| T-05 | 통화 선택을 `"GBP"`로 바꾼다 (정상 네트워크 상태) | `#status-badge`의 텍스트가 `"정상"`이다 |
| T-06 | URL에 `?fault=timeout`을 붙이고 통화 선택을 `"EUR"`로 바꾼다 | `#status-badge`에 `"오래된 데이터"`와 `"시간 초과"`가 포함되고, 직전에 정상 조회된 값이 화면에 그대로 남아있다 |
| T-07 | 통화 선택을 `"EUR"`로 바꾸고 "날짜별 기록" 표를 확인한다 | EUR로 기록된 날짜별 기록이 1건 이상 표시된다 |
| T-08 | 통화 선택을 `"EUR"`로 바꾸고 EUR 기록이 2건 이상일 때 "어제 대비 변화"를 확인한다 | 차이·방향·단위가 `EUR / 1 USD` 기준으로 표시된다 |
| T-09 | 통화 선택을 `"KRW"`로 되돌린다 | 시작 시점에 있던 KRW 날짜별 기록 3건(`2026-08-24`, `2026-08-25`, `2026-08-26`)이 그대로 보존되어 있다 (회귀 없음) |
| T-10 | `#currency-select`의 옵션 목록을 확인한다 | 정확히 4개 옵션(`KRW`,`EUR`,`JPY`,`GBP`)이 있고 각 `value` 속성이 해당 통화 코드와 같다 |

## 검사 실행 방법
1. `ex_5` 폴더에서 정적 서버를 띄운다 (예: `node scripts/serve.mjs` 또는 임의의 정적 서버로 `index.html`을 서빙).
2. 브라우저(또는 headless 도구)로 `http://localhost:<port>/` 에 접속해 위 10개를 순서대로 수동 확인한다.
3. 통과(PASS)/실패(FAIL)를 `WORKLOG.md`에 회차별로 기록한다.

> 이 표는 작업 시작 전에 고정되었습니다. 이후 삭제·완화·기대값 변경은 0건이어야 합니다 (T05-C18~C20).
